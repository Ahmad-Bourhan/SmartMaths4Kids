<% content_for :title, "Editing Question" %>

<div class="container my-5">
  <div class="card shadow-lg">
    <div class="card-header bg-warning text-dark">
      <h2 class="mb-0">✏️ Edit Question</h2>
    </div>

    <div class="card-body">
      <%= render "form", question: @question %>

      <hr>

      <div class="d-flex justify-content-between">
        <%= link_to "👁️ Show this question", @question, class: "btn btn-outline-info" %>
        <%= link_to "⬅️ Back to Questions", questions_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
